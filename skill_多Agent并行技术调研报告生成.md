# Skill：多Agent并行技术调研报告生成

> **适用场景：** 需要对多个技术领域/维度进行深度调研，并汇总为一份结构化报告（Markdown/Word）  
> **核心技巧：** 拆分专题 → 并行调研 → 统一汇总 → 标准化输出  
> **上次实践：** 《仪器仪表开关器件需求与MEMS开关竞争力调研报告》（示波器/频谱仪/矢网/MEMS开关四专题）

---

## 一、整体工作流程

```
用户需求
   │
   ▼
① 拆分专题（每个专题独立、可并行）
   │
   ▼
② 并行启动 N 个 research Agent
   │  ├── Agent-1: 专题A
   │  ├── Agent-2: 专题B
   │  ├── Agent-3: 专题C
   │  └── Agent-4: 专题D
   │
   ▼
③ 收集各Agent输出（保存至 /tmp/*.txt）
   │
   ▼
④ Python脚本汇总：清洗 + 标题层级归一化 + 拼接
   │
   ▼
⑤ 生成最终 Markdown 文件并 commit
```

---

## 二、专题拆分原则

| 原则 | 说明 | 示例 |
|------|------|------|
| **独立性** | 各专题间无强依赖，可真正并行 | 示波器章节 ≠ 频谱仪章节，互不影响 |
| **完整性** | 每个专题输出自成体系，带完整标题体系 | 每个Agent输出含`##`→`######`层级 |
| **一致的输出格式** | 统一要求Agent从`##`级标题开始 | Prompt中写明"不含顶级标题，从##开始" |
| **专题数量** | 通常3–6个，过多导致汇总复杂 | 本次4个专题刚好 |

---

## 三、Agent Prompt 编写要点

### 3.1 必须包含的要素

```markdown
你是[领域]专家。请针对[具体对象]做深度调研，
输出详细的Markdown格式内容（不含顶级标题，从二级标题##开始）。

涵盖以下内容，尽量有具体数据、产品型号、数字指标：
1. [子话题1]
2. [子话题2]
...N. [子话题N]

使用Markdown格式，包含表格。[语言]输出。数据要具体。
```

### 3.2 关键约束

| 约束 | 原因 |
|------|------|
| `从二级标题##开始` | 便于汇总时统一做 `##→###` 的层级偏移 |
| `包含表格` | 技术对比类内容表格可读性远优于列表 |
| `数据要具体` | 防止Agent输出泛泛而谈，无实际参数 |
| `[语言]输出` | 确保中英文语言一致性 |
| 明确产品型号示例 | 如"参考Menlo Micro MM5130、ADI ADGM1304" |

### 3.3 常见专题结构模板（技术竞品调研）

1. **架构/原理**：信号链图、工作原理
2. **核心需求**：对器件的量化指标要求
3. **应用场景**：使用场景罗列
4. **竞品对比表**：多维度参数横向对比
5. **主流厂商方案**：具体产品选型
6. **趋势与挑战**：面向未来

---

## 四、汇总脚本（Python）

### 4.1 读取并清洗Agent输出

```python
def read_and_clean(path):
    with open(path, 'r', encoding='utf-8') as f:
        content = f.read()
    # 去掉Agent生成的前导废话（找到第一个 ## 标题）
    idx = content.find('\n## ')
    if idx == -1:
        idx = content.find('## ')
    if idx != -1:
        content = content[idx:].lstrip()
    content = content.rstrip('- \n')
    return content
```

> **注意：** Agent输出前几行通常是"根据我的领域知识，以下是……"等废话，必须剔除。

### 4.2 标题层级归一化

当各专题作为顶级章节（`#`）的子内容时，需将各Agent输出的 `##` 整体下移一级：

```python
def bump_headings(text):
    """将 ## 整体下移一级，使其嵌套在 # 顶级章节下"""
    lines = text.split('\n')
    result = []
    for line in lines:
        if line.startswith('#'):          # 匹配所有层级的标题
            result.append('#' + line)     # 前置一个 # 即可下移一级
        else:
            result.append(line)
    return '\n'.join(result)
```

### 4.3 最终拼接模板

```python
report = f"""# 报告总标题

**字段：** 值
...

---

## 目录
...

---

## 执行摘要
（手写的跨专题汇总，含核心对比表）

---

# 章节一标题

{section_1_content}

---

# 章节二标题

{section_2_content}

---

# 综合结论

（手写的战略级结论、优先级排序、建议）

---

# 参考资料

（手写的参考文献列表）
"""
```

---

## 五、输出质量控制

### 5.1 验证清单

- [ ] 每个专题Agent输出长度 > 5000字符（内容充实）
- [ ] 每个专题包含至少1个Markdown表格
- [ ] 标题层级连续（`#` → `###`，不跳级）
- [ ] 中英文混排场景下，表格列宽合理
- [ ] 顶层有目录（带锚点链接）
- [ ] 结尾有参考文献

### 5.2 常见问题与处理

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| Agent输出有前导废话 | Agent惯性解释"接下来我将…" | `read_and_clean()`函数剔除第一个`##`前的内容 |
| 标题层级混乱 | Agent输出时`##`与`###`混用 | `bump_headings()`统一下移，而非手动修正 |
| 输出被截断（`Output too large`） | 文件保存到`/tmp/*.txt` | 用`open(path)`读取完整内容，而非直接用preview |
| 内容过于泛泛 | Prompt未给出具体产品型号示例 | Prompt中明确列出2–3个参考产品型号 |
| 各专题风格不统一 | 各Agent独立运行 | 在执行摘要和综合结论中统一口径，弱化各章节风格差异 |

---

## 六、格式选择：Markdown vs Word

| 维度 | Markdown | Word |
|------|----------|------|
| **生成难度** | 低（直接拼接字符串） | 高（需python-docx库，表格处理复杂） |
| **可读性** | GitHub/Obsidian等工具直接渲染 | 需Office软件 |
| **可维护性** | 纯文本，git diff友好 | 二进制格式，难以版本控制 |
| **表格支持** | 原生支持，简洁 | 原生支持，样式丰富 |
| **公式支持** | LaTeX语法（$...$）在多数渲染器支持 | 原生公式编辑器 |
| **图片嵌入** | 引用外部路径或Base64 | 嵌入二进制 |
| **推荐场景** | 技术报告、内部文档、GitHub仓库 | 正式交付物、客户报告 |

**结论：** 技术调研报告首选Markdown；如需交付给非技术受众，可在Markdown完成后用Pandoc转Word：

```bash
pandoc report.md -o report.docx --reference-doc=template.docx
```

---

## 七、可复用的Prompt模板库

### 7.1 竞品技术对比调研

```
你是[领域]专家。请对[技术A]在[应用场景]中的竞争力进行深度调研，
输出Markdown格式（从##开始，包含表格，数据具体，[语言]）。

结构要求：
1. 技术原理（含关键公式/架构图文字描述）
2. 核心性能指标表（参考[具体产品型号]的实测数据）
3. 主要竞争技术对比大表（列：频率范围/插损/线性度/寿命/成本/功耗）
4. 主要商业厂商深度分析（3–5家，含产品型号、市场定位、客户）
5. 在[具体应用]中的竞争力评估（优势+障碍）
6. 技术趋势与发展路线
7. 关键参考文献（8–10条，含作者/标题/期刊/年份）
```

### 7.2 仪器信号链开关需求调研

```
你是RF/微波测试仪器领域专家。请针对[仪器类型]对内部开关器件的需求做深度调研，
输出Markdown格式（从##开始，包含表格，数据具体，中文）。

结构要求：
1. [仪器]信号链架构（关键开关节点位置）
2. 对开关器件的核心需求（带宽/插损/隔离度/切换速度/线性度/寿命）
3. 主要应用场景（逐一列举，含技术参数）
4. 竞品对比分析表格（机械继电器/PIN/GaAs/SOI/MEMS，含8+列参数）
5. 主流厂商（3–5家）使用的开关方案
6. 技术趋势与挑战
```

---

## 八、经验教训总结

1. **临时文件跨Session不可靠**：`/tmp/` 文件在Session重启后会消失，需在同一Session内完成从调研到输出的完整流程，或将中间结果写入repo。

2. **并行Agent是效率关键**：4个专题串行约需40分钟，并行只需10分钟（取最慢者）。只要专题间无依赖，一定并行。

3. **Agent输出长度检查**：先用`wc -c`检查文件大小，小于3000字符说明内容不充实，需重新调整Prompt后重跑。

4. **标题归一化优于手动修正**：不要逐行手动调整标题层级，用`bump_headings()`函数一次性解决，且对所有专题保持一致。

5. **执行摘要和综合结论必须手写**：这两部分是报告的灵魂，需要跨专题的综合判断，不能由单一Agent完成。Agent只负责各章节的"原料"。

6. **表格是技术报告的杀手锏**：竞品对比如果不用表格而用列表，可读性损失70%以上。在Prompt中强制要求"包含表格"。

7. **参考文献提升可信度**：哪怕是知识蒸馏型报告，加上格式规范的参考文献列表，显著提升报告的专业形象。

---

*本Skill文档基于《仪器仪表开关器件需求与MEMS开关竞争力调研报告》生成过程总结，适用于任何多专题并行技术调研场景。*
